import { defineConfig } from "@playwright/test";

export default defineConfig({
  // Adicione suas configurações e overrides do Playwright aqui
  // Exemplo:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:8080',
  // },
});